# ============================================================================
# Tools Gateway Configuration
# ============================================================================
# ALL runtime configuration is managed via the Configuration UI.
# This file is used ONLY for initial JWT secret migration on first run.
#
# After first startup:
#   - Configure JWT, logging, services, origins via Configuration tab in UI
#   - Configure OAuth providers via Admin Security tab in UI
#
# Infrastructure settings (not in this file):
#   - Database path: Default is tools_gateway.db in current directory
#   - Host: Default is 0.0.0.0
#   - Port: Default is 8021 (set PORT environment variable to change)
# ============================================================================

# JWT Secret (for initial migration only - then managed via Configuration UI)
# On first run, this will be saved to database. After that, manage via UI.
# Generate new: openssl rand -base64 64
JWT_SECRET=AGD80F/Dp1s2m4nruBRkddaYI7pvCT9vGzhfOkSxrBo=
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=480
