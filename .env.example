# MCP Gateway Environment Configuration
# Copy this file to .env and update with your values

# =============================================================================
# Agentic Search Service Configuration (Machine A)
# =============================================================================

# Location of the Tools Gateway service
# For local development:
MCP_GATEWAY_URL=http://localhost:8021
# For distributed deployment:
# MCP_GATEWAY_URL=http://tools-gateway-machine:8021
# For load balanced deployment:
# MCP_GATEWAY_URL=https://gateway.your-domain.com

# Public URL of Agentic Search service (used for Origin header validation)
# This should match what the load balancer/reverse proxy serves
# For local development:
AGENTIC_SEARCH_URL=http://localhost:8023
# For production:
# AGENTIC_SEARCH_URL=https://search.your-domain.com

# Alternative: Explicitly set the origin header sent to gateway
# AGENTIC_SEARCH_ORIGIN=https://search.your-domain.com

# Service binding configuration
HOST=0.0.0.0
PORT=8023

# =============================================================================
# Tools Gateway Service Configuration (Machine B)
# =============================================================================

# Service binding (same on both services, but Gateway typically uses 8021)
# HOST=0.0.0.0
# PORT=8021

# Gateway doesn't need MCP_GATEWAY_URL since it IS the gateway

# =============================================================================
# Origin Validation Configuration
# =============================================================================

# These are configured in tools_gateway/config.py or via API
# Default allowed origins: localhost, 127.0.0.1, 0.0.0.0
#
# To add more at runtime:
# curl -X POST http://gateway-url/config/origin/add \
#   -H "Content-Type: application/json" \
#   -d '{"origin": "your-domain.com"}'
#
# To enable all HTTPS origins (for load balancers):
# curl -X POST http://gateway-url/config/origin \
#   -H "Content-Type: application/json" \
#   -d '{"allow_https": true}'

# =============================================================================
# Deployment Examples
# =============================================================================

# Example 1: Local Development (Single Machine)
# MCP_GATEWAY_URL=http://localhost:8021
# AGENTIC_SEARCH_URL=http://localhost:8023

# Example 2: Docker Compose (Same Network)
# MCP_GATEWAY_URL=http://tools-gateway:8021
# AGENTIC_SEARCH_URL=http://agentic-search:8023

# Example 3: Kubernetes (Same Cluster)
# MCP_GATEWAY_URL=http://tools-gateway-service:8021
# AGENTIC_SEARCH_URL=https://search.your-domain.com

# Example 4: Separate Machines
# MCP_GATEWAY_URL=http://10.0.1.50:8021
# AGENTIC_SEARCH_URL=https://search.your-domain.com

# Example 5: Full Production (Load Balanced)
# MCP_GATEWAY_URL=https://gateway.your-domain.com
# AGENTIC_SEARCH_URL=https://search.your-domain.com

# =============================================================================
# Additional Configuration
# =============================================================================

# Python logging level (optional)
# LOG_LEVEL=INFO

# Ollama configuration (for Agentic Search)
# OLLAMA_HOST=http://localhost:11434
